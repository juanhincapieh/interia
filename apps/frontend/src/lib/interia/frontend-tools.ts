/**
 * Interia frontend tools are registered in {@link useInteriaProject}
 * (`use-interia-project.tsx`) via CopilotKit `useFrontendTool`.
 */
export { useInteriaProject } from "./use-interia-project";
